import { ScrollIcon } from "@/components/atoms/icons";
import { StateMessage } from "@/components/atoms/StateMessage";
import type { Mission } from "@/types/mission";

export function MissionList({ missions, loading, onOpen }: { missions: Mission[]; loading: boolean; onOpen: (mission: Mission) => void }) {
  return <section className="pixel-panel mt-8 overflow-hidden"><header className="grid grid-cols-[1fr_auto] border-b-2 border-[#5d431d] bg-[#17160f] px-5 py-4 text-[10px] font-black uppercase tracking-wider text-[#9d947e] sm:grid-cols-[1.4fr_.7fr_.7fr_auto] sm:px-7"><span>Missão</span><span className="hidden sm:block">Evidência</span><span className="hidden sm:block">Recompensa</span><span>Ações</span></header>{loading ? <StateMessage>Carregando missões...</StateMessage> : missions.map((mission) => <article key={mission.id} className="grid grid-cols-[1fr_auto] items-center gap-4 border-b-2 border-[#302a1b] px-5 py-5 last:border-b-0 sm:grid-cols-[1.4fr_.7fr_.7fr_auto] sm:px-7"><div className="flex min-w-0 gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center border-2 border-[#72501d] bg-[#251c0f] text-gold"><ScrollIcon className="h-5 w-5" /></span><div className="min-w-0"><h2 className="truncate text-sm font-black text-[#ebdbb8]">{mission.title}</h2><p className="mt-1 text-[10px] text-[#878174]">{mission.hasProgress ? "Em andamento · edição bloqueada" : `${mission.status} · ${mission.updatedAt}`}</p></div></div><span className="hidden text-xs text-[#a9a18e] sm:block">{mission.evidenceType}</span><strong className="hidden text-xs font-black text-gold-light sm:block">{mission.xp} EXP</strong><button onClick={() => onOpen(mission)} className="border-2 border-[#78551f] bg-[#21190e] px-3 py-2 text-[10px] font-black uppercase tracking-wider text-gold-light shadow-[3px_3px_0_#060705] hover:border-gold hover:bg-[#342510]">{mission.hasProgress ? "Ver" : "Editar"}</button></article>)}</section>;
}
