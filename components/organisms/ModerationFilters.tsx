import type { Collaborator } from "@/types/moderation";

type MissionOption = { id: string; title: string };

export function ModerationFilters({ collaborators, missions, collaboratorQuery, missionId, onCollaboratorChange, onMissionChange, onClear }: { collaborators: Collaborator[]; missions: MissionOption[]; collaboratorQuery: string; missionId: string; onCollaboratorChange: (value: string) => void; onMissionChange: (value: string) => void; onClear: () => void }) {
  return <div className="border-b-2 border-[#5d431d] bg-[#17160f] p-5 sm:px-7"><p className="eyebrow">R1-02 · Filtros da fila</p><div className="mt-4 grid gap-4 lg:grid-cols-[minmax(0,1fr)_minmax(0,1fr)_auto] lg:items-end"><label><span className="mb-2 block text-[10px] font-black uppercase tracking-[0.14em] text-gold-light">Pesquisar colaborador</span><input list="moderation-collaborators" value={collaboratorQuery} onChange={(event) => onCollaboratorChange(event.target.value)} placeholder="Digite ou selecione um colaborador" className="w-full border-2 border-[#76521e] bg-[#0c0f0c] px-4 py-3 text-sm text-cream outline-none shadow-[inset_3px_3px_0_#060705] placeholder:text-[#5e5d55] focus:border-gold" /><datalist id="moderation-collaborators">{collaborators.map((item) => <option key={item.id} value={item.name} />)}</datalist></label><label><span className="mb-2 block text-[10px] font-black uppercase tracking-[0.14em] text-gold-light">Filtrar por missão</span><select value={missionId} onChange={(event) => onMissionChange(event.target.value)} className="w-full border-2 border-[#76521e] bg-[#0c0f0c] px-4 py-3 text-sm text-cream outline-none shadow-[inset_3px_3px_0_#060705] focus:border-gold"><option value="">Todas as missões</option>{missions.map((item) => <option key={item.id} value={item.id}>{item.title}</option>)}</select></label><button type="button" onClick={onClear} disabled={!collaboratorQuery && !missionId} className="pixel-button pixel-button-secondary h-[46px] px-5 py-3 text-[10px] disabled:cursor-not-allowed disabled:opacity-45">Limpar</button></div></div>;
}
